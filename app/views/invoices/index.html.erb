<h1>Invoices</h1>
<table>
  <tr>
    <th>Invoice Id</th>
    <th>Customer</th>  
  </tr>
  <%= render(@invoices) || "No Invoices" %>
  <!-- Refactored above
  <% @invoices.each do |invoice| %>
    
  <% end %>
  -->
</table>

